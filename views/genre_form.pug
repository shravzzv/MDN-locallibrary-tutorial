extends layout

block content

  h1 #{title}

  //- if a form's action attribute is undefined or empty, then the form will be submitted back to the current page URL.
  form(method='POST')
    div.form-group
      label(for='name') Genre: 
      input#name.form-control(type='text', placeholder='Fantasy, Poetry etc.' name='name' required value=(undefined===genre ? '' : genre.name) )
    button.btn.btn-primary(type='submit') Submit

  if errors
    ul
      for error in errors
        li!= error.msg